class Solution {
    public int[] frequencySort(int[] nums) {
        // brute force approach 
        //  Map<Integer, Integer> map = new HashMap<>();
        // List<Integer> ans = new ArrayList<>();
        // for (int n : nums) {                            
        //     ans.add(n);
        //     map.put(n, map.getOrDefault(n, 0) + 1);
        // }

        // Collections.sort(ans, (a, b) ->                 
        //     (map.get(a) == map.get(b))? b + a : map.get(a) - map.get(b)
        // );

        // return ans.stream().mapToInt(i -> i).toArray();



        // // first increase acc to its frequncy then count its 

        // Map<Integer, Integer> map=new HashMap<>();
        // List<Integer> ans=new ArrayList<>();
        // for(int n:nums){
        //     ans.add(n);
        //     map.put(n,map.getOrDefault(n, 0)+1);

        // }
        // Collections.sort(ans, (a, b)-> (map.get(a)==map.get(b))? b-a : map.get(a)-map.get(b));
        // return ans.stream().mapToInt(i->i).toArray();


        //using priority queue
  
        Map<Integer, Integer> freqMap = new HashMap<>();
        for (int num : nums) {
            freqMap.put(num, freqMap.getOrDefault(num, 0) + 1);
        }
        PriorityQueue<Integer> minHeap = new PriorityQueue<>((a, b) -> 
            freqMap.get(a) == freqMap.get(b) ? b - a : 
            freqMap.get(a) - freqMap.get(b));
        int index = 0;
        for(int num : nums) {
            minHeap.add(num);
        }
        while(!minHeap.isEmpty()) {
            nums[index++] = minHeap.remove();
        }
        return nums;
    }
}
        
    
